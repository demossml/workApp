.turbo
node_modules
dist

# База данных DuckDB (меняется каждые 5 минут, не коммитить)
packages/backend/data/*.duckdb
packages/backend/data/*.duckdb.wal

# Секретные файлы - НИКОГДА не коммитить!
.dev.vars
*.dev.vars
.env
.env.*
!.env.example
wrangler.toml.local

# Cloudflare Workers
.wrangler/
worker/