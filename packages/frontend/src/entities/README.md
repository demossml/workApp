# entities

Слой доменных сущностей:
- `lead`
- `operator`
- `sale`
- `report`

Каждая сущность содержит:
- `model.ts` (типы и нормализация данных)
- `api.ts` (запросы к серверу)

Фичи используют `entities`, а не сырые ответы API.
